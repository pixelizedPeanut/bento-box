## 🍣 Sushi (Frontend) Setup

The Vue.js frontend application for `bento-box`.

### Prerequisites
* Node.js (v18+)
* npm

### 1. Installation

Navigate to the frontend directory and install the dependencies:

```bash
cd sushi
npm install
